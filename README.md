# sql-set-operators-project
This project demonstrates how SQL set operators are used in real-time scenarios to compare data between multiple yearly sales tables. It includes table creation, sample data, and queries using:  UNION  UNION ALL  INTERSECT  MINUS
